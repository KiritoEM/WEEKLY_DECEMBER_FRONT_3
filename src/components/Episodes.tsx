import EpisodesHeader from "./Episodes/EpisodesHeader";

const Episodes = (): JSX.Element => {
  return (
    <section id="episodes">
      <EpisodesHeader />
    </section>
  );
};

export default Episodes;
